---
'minimum-fee-job': major
---

Change Ethereum network fee calculation: Now uses a multiplier of daily average gas price instead of the configured value (the average period and multiplier are both configurable)
